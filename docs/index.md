# PhenoRelay

PhenoRelay is a Phenopacket-native federated discovery project for sites that
want lightweight local search, controlled sharing, and optional evidence tooling.

The first implementation track keeps two surfaces:

- Rust for deployable discovery primitives.
- Python for LinkML schemas, evidence validation, documentation, and agent
  workflows.

Both surfaces should converge on the same JSON contracts.

